import  {makeStyles} from '@material-ui/core/styles';

const styles = makeStyles(theme => ({
    mapContainer: {
        width: '70%',
        height: '100%',
    }
}));

export default styles;