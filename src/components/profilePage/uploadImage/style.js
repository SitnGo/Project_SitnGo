import {makeStyles} from '@material-ui/core/styles';

const styles = makeStyles(theme => ({
    position:'relative',
    top:'2%',
    left:'41%',
    cursor:'pointer',
    width:120,
    height:120,
    zIndex:222,
}));

export default styles;