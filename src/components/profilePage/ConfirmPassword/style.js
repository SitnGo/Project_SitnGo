import  {makeStyles} from '@material-ui/core/styles';

const styles = makeStyles(theme => ({
    submit: {
        background: '#fb9403',
        color: '#282e34',
        '&:hover': {
            background: '#fb9403',
            opacity: 0.95,
        }
    },
}));

export default styles;






