import { makeStyles } from "@material-ui/core";

const styles = makeStyles(theme => ({
    card: {
        overflowY:'auto',
        maxHeight:'300px',
        width: '300px',
        margin: '5px', 
        cursor:'pointer',
    }
}))
    
export default styles;